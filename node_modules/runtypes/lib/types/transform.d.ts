import { Runtype, Static } from '../runtype';
export declare type Transformer<A extends Runtype, B extends unknown> = (x: Static<A>) => B;
export interface Transform<A extends Runtype, B extends unknown> extends Runtype<B> {
    tag: 'transform';
    underlying: A;
    transformer: Transformer<A, B>;
    name?: string;
}
export declare function Transform<A extends Runtype, B extends unknown>(underlying: A, transformer: Transformer<A, B>, options?: {
    name?: string;
}): Transform<A, B>;
