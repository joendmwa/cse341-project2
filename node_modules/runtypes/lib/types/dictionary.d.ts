import { Runtype, Static } from '../runtype';
declare type DictionaryKeyType = string | number | symbol;
declare type StringLiteralFor<K extends DictionaryKeyType> = K extends string ? 'string' : K extends number ? 'number' : K extends symbol ? 'symbol' : never;
declare type DictionaryKeyRuntype = Runtype<string | number | symbol>;
export interface Dictionary<V extends Runtype, K extends DictionaryKeyType> extends Runtype<{
    [_ in K]: Static<V>;
}> {
    tag: 'dictionary';
    key: StringLiteralFor<K>;
    value: V;
}
export interface StringDictionary<V extends Runtype> extends Runtype<{
    [_: string]: Static<V>;
}> {
    tag: 'dictionary';
    key: 'string';
    value: V;
}
export interface NumberDictionary<V extends Runtype> extends Runtype<{
    [_: number]: Static<V>;
}> {
    tag: 'dictionary';
    key: 'number';
    value: V;
}
/**
 * Construct a runtype for arbitrary dictionaries.
 * @param value - A `Runtype` for value.
 * @param [key] - A `Runtype` for key.
 */
export declare function Dictionary<V extends Runtype, K extends DictionaryKeyRuntype>(value: V, key?: K): Dictionary<V, Static<K>>;
/**
 * Construct a runtype for arbitrary dictionaries.
 * @deprecated When you want to specify `key`, pass a `Runtype` for it.
 * @param value - A `Runtype` for value.
 * @param [key] - A string representing a type for key.
 */
export declare function Dictionary<V extends Runtype>(value: V, key: 'string'): StringDictionary<V>;
/**
 * Construct a runtype for arbitrary dictionaries.
 * @deprecated When you want to specify `key`, pass a `Runtype` for it.
 * @param value - A `Runtype` for value.
 * @param [key] - A string representing a type for key.
 */
export declare function Dictionary<V extends Runtype>(value: V, key: 'number'): NumberDictionary<V>;
export {};
